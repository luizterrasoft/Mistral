/* Table: CAIXAS, Owner: SYSDBA */

CREATE TABLE CAIXAS 
(
  CX_LOJA	DOUBLE PRECISION,
  CX_CODI	DOUBLE PRECISION,
  CX_DESC	VARCHAR(20),
  CX_VALO	DOUBLE PRECISION,
  CX_HFU1	VARCHAR(5),
  CX_HFU2	VARCHAR(5)
);

/* Table: CARTOES, Owner: SYSDBA */

CREATE TABLE CARTOES 
(
  CA_CAIX	DOUBLE PRECISION,
  CA_LOJA	DOUBLE PRECISION,
  CA_VEND	DOUBLE PRECISION,
  CA_ORDE	SMALLINT,
  CA_CADM	DOUBLE PRECISION,
  CA_NUME	VARCHAR(50)
);

/* Table: CARTOES_LOCAL, Owner: SYSDBA */

CREATE TABLE CARTOES_LOCAL 
(
  CA_CAIX	DOUBLE PRECISION,
  CA_LOJA	DOUBLE PRECISION,
  CA_VEND	DOUBLE PRECISION,
  CA_ORDE	SMALLINT,
  CA_CADM	DOUBLE PRECISION,
  CA_NUME	VARCHAR(50)
);

/* Table: CHEQUES, Owner: SYSDBA */

CREATE TABLE CHEQUES 
(
  CH_CAIX	DOUBLE PRECISION,
  CH_LOJA	DOUBLE PRECISION,
  CH_VEND	DOUBLE PRECISION,
  CH_BANC	INTEGER,
  CH_NBAN	VARCHAR(30),
  CH_AGEN	VARCHAR(10),
  CH_CONT	VARCHAR(20),
  CH_NUME	DOUBLE PRECISION,
  CH_ORDE	SMALLINT,
  CH_FLAG	VARCHAR(1),
  CH_CPF	VARCHAR(14),
  CH_NOME	VARCHAR(40),
  CH_FONE	VARCHAR(15)
);

/* Table: CHEQUES_LOCAL, Owner: SYSDBA */

CREATE TABLE CHEQUES_LOCAL 
(
  CH_CAIX	DOUBLE PRECISION,
  CH_LOJA	DOUBLE PRECISION,
  CH_VEND	DOUBLE PRECISION,
  CH_BANC	INTEGER,
  CH_NBAN	VARCHAR(30),
  CH_AGEN	VARCHAR(10),
  CH_CONT	VARCHAR(20),
  CH_NUME	DOUBLE PRECISION,
  CH_FLAG	VARCHAR(1),
  CH_CPF	VARCHAR(14),
  CH_NOME	VARCHAR(40),
  CH_FONE	VARCHAR(15),
  CH_ORDE	SMALLINT
);

/* Table: CONF, Owner: SYSDBA */

CREATE TABLE CONF 
(
  EMPRESA	VARCHAR(60),
  SECURITY	INTEGER,
  ENDERECO	VARCHAR(60),
  BAIRRO	VARCHAR(30),
  CIDADE	VARCHAR(30),
  UF	VARCHAR(2),
  CEP	VARCHAR(10),
  CNPJ	VARCHAR(18),
  TELEFONE	VARCHAR(15),
  FAX	VARCHAR(15),
  INSC	VARCHAR(15),
  BANCO_DE_DADOS	INTEGER,
  NIVEL_PERMISSAO	INTEGER,
  FORMATO_DATA	INTEGER
);

/* Table: CREDIARIOS, Owner: SYSDBA */

CREATE TABLE CREDIARIOS 
(
  CR_CAIX	DOUBLE PRECISION,
  CR_LOJA	DOUBLE PRECISION,
  CR_VEND	DOUBLE PRECISION,
  CR_ORDE	SMALLINT,
  CR_CLIE	DOUBLE PRECISION,
  CR_CODI	DOUBLE PRECISION,
  CR_TOTA	DOUBLE PRECISION,
  CR_VENT	DOUBLE PRECISION,
  CR_FINA	DOUBLE PRECISION,
  CR_RECE	DOUBLE PRECISION,
  CR_CPF	VARCHAR(14),
  CR_NOME	VARCHAR(40)
);

/* Table: CREDIARIOS_LOCAL, Owner: SYSDBA */

CREATE TABLE CREDIARIOS_LOCAL 
(
  CR_CAIX	DOUBLE PRECISION,
  CR_LOJA	DOUBLE PRECISION,
  CR_VEND	DOUBLE PRECISION,
  CR_ORDE	SMALLINT,
  CR_CLIE	DOUBLE PRECISION,
  CR_CODI	DOUBLE PRECISION,
  CR_TOTA	DOUBLE PRECISION,
  CR_VENT	DOUBLE PRECISION,
  CR_FINA	DOUBLE PRECISION,
  CR_RECE	DOUBLE PRECISION,
  CR_CPF	VARCHAR(14),
  CR_NOME	VARCHAR(40)
);

/* Table: FORMAS_PAGAMENTO, Owner: SYSDBA */

CREATE TABLE FORMAS_PAGAMENTO 
(
  FO_CODI	DOUBLE PRECISION,
  FO_DESC	VARCHAR(30)
);

/* Table: HISTORICO_SITUACOES, Owner: SYSDBA */

CREATE TABLE HISTORICO_SITUACOES 
(
  HS_LOJA	DOUBLE PRECISION,
  HS_CAIX	DOUBLE PRECISION,
  HS_DATA	DATETIME,
  HS_STAT	VARCHAR(1),
  HS_HORA	VARCHAR(5)
);

/* Table: ITENS_VENDAS, Owner: SYSDBA */

CREATE TABLE ITENS_VENDAS 
(
  IV_LOJA	DOUBLE PRECISION,
  IV_CAIX	DOUBLE PRECISION,
  IV_VEND	DOUBLE PRECISION,
  IV_TIPZ	VARCHAR(1),
  IV_GRUP	DOUBLE PRECISION,
  IV_SUBG	DOUBLE PRECISION,
  IV_PROD	DOUBLE PRECISION,
  IV_TAMA	VARCHAR(5),
  IV_CORE	DOUBLE PRECISION,
  IV_QTDE	DOUBLE PRECISION,
  IV_TOTA	DOUBLE PRECISION,
  IV_PVEN	DOUBLE PRECISION,
  IV_OTAM	SMALLINT
);

/* Table: ITENS_VENDAS_LOCAL, Owner: SYSDBA */

CREATE TABLE ITENS_VENDAS_LOCAL 
(
  IV_LOJA	DOUBLE PRECISION,
  IV_CAIX	DOUBLE PRECISION,
  IV_VEND	DOUBLE PRECISION,
  IV_TIPZ	VARCHAR(1),
  IV_GRUP	DOUBLE PRECISION,
  IV_SUBG	DOUBLE PRECISION,
  IV_PROD	DOUBLE PRECISION,
  IV_TAMA	VARCHAR(5),
  IV_CORE	DOUBLE PRECISION,
  IV_QTDE	DOUBLE PRECISION,
  IV_TOTA	DOUBLE PRECISION,
  IV_PVEN	DOUBLE PRECISION,
  IV_OTAM	SMALLINT
);

/* Table: LANCAMENTOS_SESSAO_CAIXA, Owner: SYSDBA */

CREATE TABLE LANCAMENTOS_SESSAO_CAIXA 
(
  LA_LOJA	DOUBLE PRECISION,
  LA_CAIX	DOUBLE PRECISION,
  LA_TIPO	VARCHAR(1),
  LA_VALO	DOUBLE PRECISION,
  LA_CDES	DOUBLE PRECISION,
  LA_DATA	DATETIME,
  LA_HORA	VARCHAR(5)
);

/* Table: OPERADORES, Owner: SYSDBA */

CREATE TABLE OPERADORES 
(
  OP_CODI	DOUBLE PRECISION,
  OP_NOME	VARCHAR(40),
  OP_STAT	VARCHAR(1),
  OP_SENH	VARCHAR(8),
  OP_TIPO	VARCHAR(1)
);

/* Table: PAGAMENTOS_A_VISTA, Owner: SYSDBA */

CREATE TABLE PAGAMENTOS_A_VISTA 
(
  PV_CAIX	DOUBLE PRECISION,
  PV_LOJA	DOUBLE PRECISION,
  PV_VEND	DOUBLE PRECISION,
  PV_FPGT	DOUBLE PRECISION,
  PV_VALO	DOUBLE PRECISION,
  PV_TROC	DOUBLE PRECISION
);

/* Table: PAGAMENTOS_A_VISTA_LOCAL, Owner: SYSDBA */

CREATE TABLE PAGAMENTOS_A_VISTA_LOCAL 
(
  PV_CAIX	DOUBLE PRECISION,
  PV_LOJA	DOUBLE PRECISION,
  PV_VEND	DOUBLE PRECISION,
  PV_FPGT	DOUBLE PRECISION,
  PV_VALO	DOUBLE PRECISION,
  PV_TROC	DOUBLE PRECISION
);

/* Table: SANGRIAS, Owner: SYSDBA */

CREATE TABLE SANGRIAS 
(
  SA_LOJA	DOUBLE PRECISION,
  SA_CAIX	DOUBLE PRECISION,
  SA_TIPO	VARCHAR(1),
  SA_VALO	DOUBLE PRECISION,
  SA_CDES	DOUBLE PRECISION,
  SA_DATA	DATETIME,
  SA_HORA	VARCHAR(5)
);

/* Table: SESSAO_CAIXA, Owner: SYSDBA */

CREATE TABLE SESSAO_CAIXA 
(
  SC_LOJA	DOUBLE PRECISION,
  SC_CAIX	DOUBLE PRECISION,
  SC_DATA	DATETIME,
  SC_HORA	VARCHAR(5),
  SC_HORF	VARCHAR(5),
  SC_OPER	DOUBLE PRECISION,
  SC_VALA	DOUBLE PRECISION,
  SC_VALF	DOUBLE PRECISION,
  SC_VALO	DOUBLE PRECISION,
  SC_STAT	VARCHAR(1)
);

/* Table: SITUACOES_CAIXA, Owner: SYSDBA */

CREATE TABLE SITUACOES_CAIXA 
(
  SI_CODI	VARCHAR(1),
  SI_DESC	VARCHAR(30)
);

/* Table: VENDAS, Owner: SYSDBA */

CREATE TABLE VENDAS 
(
  VE_CAIX	DOUBLE PRECISION,
  VE_LOJA	DOUBLE PRECISION,
  VE_CODI	DOUBLE PRECISION,
  VE_DATA	DATETIME,
  VE_OPER	DOUBLE PRECISION,
  VE_CLIE	DOUBLE PRECISION,
  VE_CONT	DOUBLE PRECISION,
  VE_TOTA	DOUBLE PRECISION,
  VE_QTDE	DOUBLE PRECISION,
  VE_STAT	VARCHAR(1),
  VE_TIPZ	VARCHAR(1),
  VE_CUPO	DOUBLE PRECISION,
  VE_VEND	DOUBLE PRECISION,
  VE_ALIQ	DOUBLE PRECISION,
  VE_ICMS	DOUBLE PRECISION,
  VE_FONE	VARCHAR(15),
  VE_NOME	VARCHAR(40),
  VE_CPF	VARCHAR(14),
  VE_SERI	VARCHAR(1),
  VE_NOTA	VARCHAR(10),
  VE_DESC	DOUBLE PRECISION,
  VE_PDES	DOUBLE PRECISION
);

/* Table: VENDAS_FORMA_PAGAMENTO, Owner: SYSDBA */

CREATE TABLE VENDAS_FORMA_PAGAMENTO 
(
  VF_CAIX	DOUBLE PRECISION,
  VF_LOJA	DOUBLE PRECISION,
  VF_VEND	DOUBLE PRECISION,
  VF_ORDE	SMALLINT,
  VF_FPGT	DOUBLE PRECISION,
  VF_VALO	DOUBLE PRECISION
);

/* Table: VENDAS_FORMA_PAGAMENTO_LOCAL, Owner: SYSDBA */

CREATE TABLE VENDAS_FORMA_PAGAMENTO_LOCAL 
(
  VF_CAIX	DOUBLE PRECISION,
  VF_LOJA	DOUBLE PRECISION,
  VF_VEND	DOUBLE PRECISION,
  VF_ORDE	SMALLINT,
  VF_FPGT	DOUBLE PRECISION,
  VF_VALO	DOUBLE PRECISION
);

/* Table: VENDAS_LOCAL, Owner: SYSDBA */

CREATE TABLE VENDAS_LOCAL 
(
  VE_CAIX	DOUBLE PRECISION,
  VE_LOJA	DOUBLE PRECISION,
  VE_CODI	DOUBLE PRECISION,
  VE_DATA	DATETIME,
  VE_OPER	DOUBLE PRECISION,
  VE_CLIE	DOUBLE PRECISION,
  VE_CONT	DOUBLE PRECISION,
  VE_TOTA	DOUBLE PRECISION,
  VE_QTDE	DOUBLE PRECISION,
  VE_STAT	VARCHAR(1),
  VE_TIPZ	VARCHAR(1),
  VE_CUPO	DOUBLE PRECISION,
  VE_VEND	DOUBLE PRECISION,
  VE_ALIQ	DOUBLE PRECISION,
  VE_ICMS	DOUBLE PRECISION,
  VE_FONE	VARCHAR(15),
  VE_NOME	VARCHAR(40),
  VE_CPF	VARCHAR(14),
  VE_SERI	VARCHAR(1),
  VE_NOTA	VARCHAR(10),
  VE_DESC	DOUBLE PRECISION,
  VE_PDES	DOUBLE PRECISION
);

/*  Index definitions for all user tables */

CREATE INDEX HISTORICO_SITUACOES_PRIM ON HISTORICO_SITUACOES(HS_LOJA, HS_CAIX, HS_DATA);
CREATE INDEX LANCAMENTOS_SESSAO_CAIXA_PRIM ON LANCAMENTOS_SESSAO_CAIXA(LA_LOJA, LA_CAIX, LA_DATA);
CREATE INDEX SITUACOES_CAIXA_PRIM ON SITUACOES_CAIXA(SI_CODI);

