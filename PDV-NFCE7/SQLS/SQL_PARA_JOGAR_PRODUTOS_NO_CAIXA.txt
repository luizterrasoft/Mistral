CREATE TABLE PRODUTOS 
(
  PR_GRUP	DOUBLE PRECISION,
  PR_SUBG	DOUBLE PRECISION,
  PR_CODI	DOUBLE PRECISION,
  PR_FORN	DOUBLE PRECISION,
  PR_DESC	VARCHAR(60),
  PR_UNID	VARCHAR(5),
  PR_DMIN	DOUBLE PRECISION,
  PR_DMAX	DOUBLE PRECISION,
  PR_TTAM	DOUBLE PRECISION,
  PR_TUNI	VARCHAR(1),
  PR_CUNI	VARCHAR(1),
  PR_REFI	VARCHAR(12),
  PR_QTMI	INTEGER,
  PR_REFF	VARCHAR(15),
  PR_ULC1	TIMESTAMP,
  PR_ULC2	TIMESTAMP,
  PR_DCAD	TIMESTAMP,
  PR_LINH	DOUBLE PRECISION,
  PR_COLE	DOUBLE PRECISION,
  PR_PAR1	SMALLINT,
  PR_PAR2	SMALLINT,
  PR_MAT1	VARCHAR(15),
  PR_MAT2	VARCHAR(15),
  PR_MAT3	VARCHAR(15),
  PR_MODE	DOUBLE PRECISION
);

CREATE TABLE PRODUTOS_FAIXAS 
(
  PF_GRUP	DOUBLE PRECISION,
  PF_SUBG	DOUBLE PRECISION,
  PF_PROD	DOUBLE PRECISION,
  PF_TAMA	VARCHAR(5),
  PF_SEQ	INTEGER,
  PF_ORDE	SMALLINT
);

CREATE TABLE PRODUTOS_MATERIAIS 
(
  PM_GRUP	DOUBLE PRECISION,
  PM_SUBG	DOUBLE PRECISION,
  PM_PROD	DOUBLE PRECISION,
  PM_MATE	DOUBLE PRECISION,
  PM_ORDE	SMALLINT
);

CREATE TABLE GRUPOS_PRODUTOS 
(
  GR_CODI	DOUBLE PRECISION,
  GR_DESC	VARCHAR(20),
  GR_CUNI	VARCHAR(1),
  GR_TUNI	VARCHAR(1),
  GR_DMIN	DOUBLE PRECISION,
  GR_DMAX	DOUBLE PRECISION,
  GR_MARG	DOUBLE PRECISION,
  GR_REFI	VARCHAR(12)
);

CREATE TABLE SUBGRUPOS_PRODUTOS 
(
  SG_CODI	DOUBLE PRECISION,
  SG_DESC	VARCHAR(20),
  SG_UNID	VARCHAR(5),
  SG_REFI	VARCHAR(10),
  SG_CUNI	VARCHAR(1),
  SG_TUNI	VARCHAR(1),
  SG_DMIN	DOUBLE PRECISION,
  SG_DMAX	DOUBLE PRECISION,
  SG_MARG	DOUBLE PRECISION,
  SG_GRUP	DOUBLE PRECISION,
  SG_MAT1	VARCHAR(15),
  SG_MAT2	VARCHAR(15),
  SG_MAT3	VARCHAR(15),
  SG_TTAM	DOUBLE PRECISION,
  SG_PAR1	SMALLINT,
  SG_PAR2	SMALLINT,
  SG_PAR3	SMALLINT,
  SG_PAR4	SMALLINT,
  SG_PA21	SMALLINT,
  SG_PA22	SMALLINT,
  SG_COLE	DOUBLE PRECISION,
  SG_LINH	DOUBLE PRECISION
);

CREATE TABLE SUBGRUPOS_FAIXAS 
(
  SF_TAMA	VARCHAR(5),
  SF_GRUP	DOUBLE PRECISION,
  SF_SUBG	DOUBLE PRECISION,
  SF_SEQ	INTEGER,
  SF_ORDE	SMALLINT
);

CREATE TABLE TAMANHOS 
(
  TM_CODI	DOUBLE PRECISION,
  TM_DESC	VARCHAR(30),
  TM_SEQB	INTEGER
);

CREATE TABLE TAMANHOS_FAIXAS 
(
  TF_CODI	DOUBLE PRECISION,
  TF_TAMA	VARCHAR(5),
  TF_SEQ	INTEGER,
  TF_ORDE	SMALLINT
);


CREATE TABLE COLECOES 
(
  CO_CODI	DOUBLE PRECISION,
  CO_DESC	VARCHAR(40)
);


CREATE TABLE LINHAS
(
  LI_CODI	DOUBLE PRECISION,
  LI_DESC	VARCHAR(40)
);

CREATE TABLE MODELOS
(
  MD_CODI	DOUBLE PRECISION,
  MD_DESC	VARCHAR(40)
);

CREATE TABLE MATERIAIS 
(
  MA_CODI	DOUBLE PRECISION,
  MA_DESC	VARCHAR(40)
);

CREATE TABLE ESTOQUE 
(
  ES_GRUP	DOUBLE PRECISION,
  ES_SUBG	DOUBLE PRECISION,
  ES_PROD	DOUBLE PRECISION,
  ES_CORE	DOUBLE PRECISION,
  ES_TAMA	VARCHAR(5),
  ES_REFF	VARCHAR(15),
  ES_DESC	VARCHAR(80),
  ES_CBAR	VARCHAR(14),
  ES_ULC1	TIMESTAMP,
  ES_ULC2	TIMESTAMP,
  ES_OTAM	SMALLINT
);

CREATE TABLE ITENS_TABELAS_PRECOS 
(
  IP_TABE	DOUBLE PRECISION,
  IP_GRUP	DOUBLE PRECISION,
  IP_SUBG	DOUBLE PRECISION,
  IP_PROD	DOUBLE PRECISION,
  IP_CORE	DOUBLE PRECISION,
  IP_TAMA	VARCHAR(5),
  IP_PRE1	DOUBLE PRECISION,
  IP_PRE2	DOUBLE PRECISION,
  IP_LOJA	DOUBLE PRECISION,
  IP_REAJ	DOUBLE PRECISION,
  IP_POR1	DOUBLE PRECISION,
  IP_POR2	DOUBLE PRECISION,
  IP_OTAM	INTEGER,
  IP_REA1	DOUBLE PRECISION,
  IP_REA2	DOUBLE PRECISION
);

CREATE TABLE TABELAS_PRECOS 
(
  TP_CODI	DOUBLE PRECISION,
  TP_DESC	VARCHAR(40),
  TP_LOJA	DOUBLE PRECISION,
  TP_DAT1	TIMESTAMP,
  TP_DAT2	TIMESTAMP,
  TP_TIPO	VARCHAR(1),
  TP_VALI	TIMESTAMP
);

CREATE TABLE CORES 
(
  CR_ABRV	VARCHAR(5),
  CR_DESC	VARCHAR(30),
  CR_CODI	DOUBLE PRECISION,
  CR_SORT	VARCHAR(1)
);

CREATE TABLE FORNECEDORES 
(
  FO_CODI	DOUBLE PRECISION,
  FO_RAZA	VARCHAR(40),
  FO_NFAN	VARCHAR(30)
);

CREATE TABLE USERS 
(
  USCODE	DOUBLE PRECISION,
  USALIAS	VARCHAR(10),
  USNAME	VARCHAR(40),
  USPASSWORD	VARCHAR(6),
  USTYPE	VARCHAR(1),
  USDCAD	TIMESTAMP,
  USSESSION	DOUBLE PRECISION,
  USGRUPO	DOUBLE PRECISION
);

CREATE TABLE BANCOS 
(
  BA_CODI	DOUBLE PRECISION,
  BA_NOME	VARCHAR(40),
  BA_SITE	VARCHAR(80)
);

CREATE TABLE ALIQUOTAS_PRODUTOS 
(
  AP_GRUP	DOUBLE PRECISION,
  AP_SUBG	DOUBLE PRECISION,
  AP_PROD	DOUBLE PRECISION,
  AP_ESTA	VARCHAR(2),
  AP_ICMS	DOUBLE PRECISION,
  AP_ISUB	DOUBLE PRECISION
);

CREATE TABLE CONFIG_CODIGO_BARRAS 
(
  CAMPO	SMALLINT,
  ESPACO	SMALLINT
);

CREATE TABLE ADMINISTRADORAS_LOJAS
(
 AL_CADM double precision,
 AL_LOJA double precision,
 AL_TAXA double precision,
 AL_DIAS smallint
);

DROP TABLE TROCOS;
DROP TABLE TROCOS_LOCAL;
DROP TABLE OPERADORES;
DROP TABLE SANGRIAS;
DROP TABLE SESSAO_CAIXA;
DROP TABLE SITUACOES_CAIXA;
DROP TABLE PAGAMENTOS_A_VISTA;
DROP TABLE PAGAMENTOS_A_VISTA_LOCAL;
DROP TABLE HISTORICO_SITUACOES;
