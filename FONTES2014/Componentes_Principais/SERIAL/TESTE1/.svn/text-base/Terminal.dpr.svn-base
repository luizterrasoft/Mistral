program Terminal;

uses
  Forms,
  Main in 'Main.pas' {Form1},
  comsetup in 'comsetup.pas' {frmSetup};

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TfrmSetup, frmSetup);
  Application.Run;
end.
